package usam.action;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

public class ProveedorAction extends org.apache.struts.action.Action {

    private static final String entrarPROV = "entrar";
    private static final String salirPROV = "salir";
    private static final String errorPROV = "error";

    @Override
    public ActionForward execute(ActionMapping map, ActionForm form, HttpServletRequest request, HttpServletResponse response)
            throws Exception {
        return null;
        
    }
    }
